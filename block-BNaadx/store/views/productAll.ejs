<!DOCTYPE html>
<html>
  <head>
    <title >All Products</title>
    <link rel='stylesheet' href='../output.css'>
  </head>
  <body>
    <button class=" bg-red-500 hover:bg-red-700 text-white font-bold py-2 p-12 m-4 absolute left-2 top-4"> <a href="/">HOME</a></button> </body>

    <h1 class="font-serif text-4xl text-center p-16">You Are Viewing All Products</h1>

    <div class="p-2 m-4 flex justify-center flex-wrap">
       <% products.forEach(product => { %>
        <div class="p-4 m-4 w-4/12 divide-solid border-2 border-grey-400 flex flex-col justify-start">
            <h3><span class="m-6 font-bold text-red-800">NAME: </span> <span class="underline font-bold"><%= product.name %></h3>
            <p><span class="m-6 font-bold text-red-800">QUANTITY: </span> <%= product.quantity %></p>
            <h5><span class="m-6 font-bold text-red-800">PRICE: </span> INR <%= product.price %></h5>
            <img class="m-6" src="<%= product.image %>" width="100" height="100">
            <div><span class="m-6 font-bold text-red-800">LIKES: </span> <%= product.likes %></div>
            <button class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 m-6 rounded-l"><a href="/product/<%= product._id %>">VIEW</a></button>
        </div>
       <% }) %>
    </div>
  </body>
</html>
